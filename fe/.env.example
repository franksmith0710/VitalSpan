# 后端 API 基址（生产 build / 直连）；本地 dev 默认走 vite proxy /api
VITE_API_BASE_URL=http://localhost:8000

# H1：治理侧栏。默认关闭；设为 1 时显示「治理」分组（未对接真实总线）
# VITE_GOV_NAV=1

# 看板编辑：像素画布 v2（1440px 基准 + 拖拽/缩放）。本地开发建议开启
VITE_DASHBOARD_PIXEL_CANVAS=true
