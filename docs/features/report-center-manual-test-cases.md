# 报表中心 人工验收测试用例

> 在 VitalSpan 管理端 **手动操作** 的测试说明书 · 最近更新 2026-07-30  
> 配套：[报表中心使用指南](../user-guide/report-center-how-to.md) · [F08-RPT](../automate/prd/F08-RPT.md) · [UX backlog](../ux-critique/2026-07-30-report-center-backlog.md)

## 用法

- 每条 Case 独立执行，在记录表打 ✓ / ✗ 并备注实际现象
- 前置条件不满足时标记 **阻塞**，不强行判失败
- 自动回归对照：`cd fe && npx vitest run src/pages/admin/reports/`

---

## 准备工作（一次性）

| 项 | 要求 |
|----|------|
| 环境 | 后端 `uvicorn` + 前端 `pnpm dev` 已启动 |
| 访问 | `http://localhost:5173/admin` |
| 账号 | 至少准备 **admin**、**analyst**（或 viewer）各一；开发密码见 `backend/.env.example` |
| 数据 | 种子数据含至少 1 个报表 catalog 模板 + 1 条预制 binding（默认 dev 环境通常已有） |

---

## 第一组：导航与权限（IA）

> **目标**：侧栏入口、角色可见性与页标题一致  
> **观察点**：菜单文案、403/不可见项

### Case 1.1 — analyst 可见消费入口

**操作步骤**：

1. 使用 analyst 账号登录。
2. 展开侧栏 **报表 → 报表中心**。
3. 依次进入 **全部报表**、**预制报表**。

**预期**：

- 子菜单含 **全部报表**、**预制报表**。
- **不含** **报表模板**、**报表调度**。
- **全部报表** 页标题为「全部报表」；**预制报表** 页标题为「预制分析报表」。

---

### Case 1.2 — admin 可见管理入口

**操作步骤**：

1. 使用 admin 账号登录。
2. 展开 **报表 → 报表中心**。

**预期**：

- 子菜单额外含 **报表模板**、**报表调度**。
- 进入 **报表调度** 页描述为「管理报表定时任务，查看执行历史与失败重试。」（无内部编号）。

---

## 第二组：全部报表 Hub（消费侧）

> **目标**：找模板、默认报表、预制预览  
> **PRD**：RPT-002 / RPT-004

### Case 2.1 — 搜索与格式筛选

**操作步骤**：

1. admin 或 analyst 进入 **全部报表**。
2. 在 **授权报表模板** 搜索框输入已知模板名称关键字。
3. 点击 **PDF**（或 Word / Excel）筛选按钮。

**预期**：

- 搜索框 placeholder 为「搜索报表名称…」（不含 templateKey 字样）。
- 匹配项减少；无匹配时显示「无匹配报表」。
- 格式筛选后仅显示对应类型卡片；卡片副标题为「PDF 报表」等可读文案。

---

### Case 2.2 — 打开并运行授权模板

**操作步骤**：

1. 在 Hub 某模板卡片点击 **打开并运行**。
2. 等待页面加载。

**预期**：

- 进入浏览页，标题为模板名称。
- 自动出现 **运行结果** 表格（或占位友好文案，非工程报错口吻）。
- 页头有 **返回全部报表** 链至 `/admin/reports/center`。
- 页底 **报表导出** 无「模板 ID」输入框（消费侧隐藏 UUID）。

---

### Case 2.3 — 角色默认报表卡片

**操作步骤**：

1. 在 **系统 → 用户/角色** 为某角色绑定默认报表模板（若环境支持）。
2. 使用该角色账号打开 **全部报表**。

**预期**：

- 加载过程中 Hub 顶区有骨架占位（非空白闪烁后消失）。
- 配置正确时显示 **角色默认报表** + **打开默认报表**。
- 若 ID 已绑定但目录无节点，显示「默认模板暂未在目录中同步」兜底（非静默无卡片）。

---

### Case 2.4 — Hub 预制预览展开

**操作步骤**：

1. 在 **预制分析** 区块确认预览条数。
2. 若总数 > 6，点击 **展开全部 (N)**，再点 **收起**。

**预期**：

- 默认最多 6 条；展开后显示全部预制项。
- **进入预制报表** 链至 `/admin/reports`。

---

## 第三组：预制分析报表

> **目标**：列表、搜索、运行、深链  
> **PRD**：RPT-002

### Case 3.1 — 列表搜索与运行

**操作步骤**：

1. 进入 **预制报表**。
2. 在工具栏搜索框输入 displayName 片段。
3. 点击某行 **运行**。

**预期**：

- 列表为表格壳层（非旧版小卡片 ScrollArea）。
- 显示「筛选结果 N 条」。
- 仅被点击行按钮为「运行中…」，其他行 **运行** 仍可点。
- 下方出现 **运行结果** 表格。

---

### Case 3.2 — Hub 深链自动运行

**操作步骤**：

1. 从 Hub 预制行点击 **运行**（或手动访问 `/admin/reports?binding={bindingKey}`）。
2. 观察是否自动执行。

**预期**：

- 进入预制页后自动调用运行，结果区展示数据。
- 同一 binding 仅自动运行一次（刷新可再触发）。

---

### Case 3.3 — viewer 无权运行（若环境可配）

**操作步骤**：

1. 使用 viewer 登录，对限制角色的预制项点击 **运行**。

**预期**：

- 显示 **无权运行预制报表** 空态，非未处理 API 报错。

---

### Case 3.4 — 返回链与导出区位置（admin）

**操作步骤**：

1. admin 进入 **预制报表**，未运行前观察页面结构。
2. 运行一条后再次观察。

**预期**：

- 页头有 **返回全部报表**。
- **报表导出** 在 **运行结果** 之后；未运行前导出按钮禁用并提示需先运行（管理员可见模板 ID 字段）。

---

## 第四组：报表导出

> **目标**：消费侧导出闭环  
> **PRD**：RPT-001 / API-005

### Case 4.1 — 浏览页导出 PDF

**操作步骤**：

1. 从 Hub **打开并运行** 某 PDF 模板。
2. 在 **报表导出** 选择 **PDF**，点击 **发起导出**。

**预期**：

- 状态显示中文（如「处理中」「已完成」）。
- 成功后出现 **下载** 按钮（或明确失败 toast，非 silent fail）。

---

## 第五组：报表模板（管理侧）

> **目标**：目录、扩展、预览  
> **PRD**：RPT-003 / RPT-004 / RPT-006

### Case 5.1 — 新建模板并保存扩展

**操作步骤**：

1. admin 进入 **报表模板**。
2. 点击 **新建模板**，在左侧选中新建节点。
3. 打开 **扩展配置** Tab，添加指标，填写 **变更说明**，保存。

**预期**：

- toast「扩展配置已保存」。
- **预览** Tab 可看到渲染结果变化（或占位说明）。

---

### Case 5.2 — 模板块编辑

**操作步骤**：

1. 选中 Word/Excel/PDF 模板，进入 **模板块** Tab。
2. 做一处块结构修改并保存。

**预期**：

- 保存成功反馈；再次进入内容保持。

---

## 第六组：报表调度（管理侧）

> **目标**：列表、历史、中文状态  
> **PRD**：RPT-005

### Case 6.1 — 调度列表与状态文案

**操作步骤**：

1. admin 进入 **报表调度**。
2. 查看 **状态** 列 Badge。

**预期**：

- 状态为中文（如 **已调度**、**已暂停**），非 `scheduled`  raw 英文。

---

### Case 6.2 — 执行历史与重试

**操作步骤**：

1. 点击某调度行的模板名称展开历史。
2. 对失败记录（若有）点击 **重试**。

**预期**：

- 历史 **状态** 列为中文（如 **执行失败**、**已生成（投递降级）**）。
- 重试提交后有成功 toast 或明确错误提示。

---

## 第七组：异常与回归

### Case 7.1 — API 失败可恢复

**操作步骤**：

1. 停止后端或模拟网络错误。
2. 刷新 **全部报表** 或 **预制报表**。

**预期**：

- 页顶 **PageErrorBanner** 显示可读错误信息。
- 点击 **重试** 可重新加载（恢复后端后）。

---

### Case 7.2 — 空目录空态

**操作步骤**：

1. 在无 catalog 模板的环境（或隔离测试库）打开 **全部报表**。

**预期**：

- **暂无授权报表** 空态，含引导「浏览预制报表」按钮。

---

## 评分汇总

| 组别 | 主题 | Case 数 | 通过 | 失败 | 阻塞 |
|------|------|---------|------|------|------|
| 第一组 | 导航与权限 | 2 | 1 | 0 | 1 |
| 第二组 | 全部报表 Hub | 4 | 3 | 0 | 1 |
| 第三组 | 预制分析 | 4 | 2 | 1 | 1 |
| 第四组 | 报表导出 | 1 | 1 | 0 | 0 |
| 第五组 | 报表模板 | 2 | 0 | 0 | 2 |
| 第六组 | 报表调度 | 2 | 1 | 0 | 1 |
| 第七组 | 异常与回归 | 2 | 1 | 0 | 1 |
| **合计** | | **17** | **9** | **1** | **7** |

---

## 执行记录 · 2026-07-30

> **执行人**：Agent（Vitest + Browser @ `localhost:5176` + API 抽检）  
> **环境**：后端 `:8000` ✓ · 前端 Vite **`:5176`**（5173 被占用）· 账号 `admin/analyst/viewer` + `changeme`  
> **数据**：执行中 API 注入 1 模板 + 1 预制 binding + 1 调度（扩展/实体表未完整，影响运行类 Case）

### 自动回归

| 命令 | 结果 |
|------|------|
| `npx vitest run src/pages/admin/reports/` | **21/21 通过** |
| `pytest tests/test_ff_rpt_companion_e95d.py tests/test_m10_report_templates_r234.py` | **27/28 通过**（1 条 ACL 码断言漂移，与 UI 无关） |

### 逐条结果

| Case | 结果 | 证据 / 备注 |
|------|------|-------------|
| **1.1** analyst 消费入口 | ⏸ 阻塞 | 本轮未切换 analyst 浏览器会话；`resolve-nav.test.ts` 已覆盖导航契约 |
| **1.2** admin 管理入口 | ✅ | 侧栏含「报表模板」「报表调度」；调度页描述无 RPT-005 |
| **2.1** 搜索与格式筛选 | ✅ | placeholder「搜索报表名称…」；PDF 筛选；副标题「PDF 报表」；Vitest 覆盖无匹配 |
| **2.2** 打开并运行 | ⚠ 部分 | 「返回全部报表」✓；导出区无「模板 ID」✓；**运行结果未出现**（API：`RPT_ENGINE_DATASOURCE_REQUIRED`） |
| **2.3** 默认报表卡片 | ⏸ 阻塞 | 未配置角色 default-views |
| **2.4** Hub 预制预览 | ✅ | 「进入预制报表」链正确；仅 1 条预制，无需「展开全部」 |
| **3.1** 预制搜索与运行 | ⚠ 部分 | ListKit 表格 +「筛选结果 1 条」✓；**运行无结果表**（API：`RPT_PREFAB_ENTITY_NOT_READY`） |
| **3.2** 深链 auto-run | ⏸ 阻塞 | 依赖预制 run 成功；Vitest mock 路径 **通过** |
| **3.3** viewer 无权 | ⏸ 阻塞 | 未切换 viewer 会话；Vitest **通过** |
| **3.4** 返回链与导出位置 | ✅ | 「返回全部报表」✓；导出在结果后且未运行前 disabled + 提示文案 ✓ |
| **4.1** 浏览页导出 PDF | ✅ | API 导出 `status=ready` 且返回 downloadUrl；浏览页有「发起导出」无 UUID |
| **5.1** 新建模板扩展 | ⏸ 阻塞 | 未做完整 UI 新建流（避免污染库）；Vitest 扩展保存 **通过** |
| **5.2** 模板块编辑 | ⏸ 阻塞 | 同上；后端 companion pytest **通过** |
| **6.1** 调度中文状态 | ✅ | 列表显示「草稿」（非 raw `draft`）；描述文案正确 |
| **6.2** 执行历史重试 | ⏸ 阻塞 | 无执行历史记录可展开；需先 semi-real execute |
| **7.1** API 失败可恢复 | ⏸ 阻塞 | 未停后端（避免干扰 dev 环境） |
| **7.2** 空目录空态 | ✅ | 初始空库时已验证「暂无授权报表」+ 引导按钮（后注入数据） |

### 结论

- **UI / 导航 / 文案 / 壳层**：与用例预期一致（含近期 UX 修复项）。
- **运行类主路径**（模板 run、预制 run、调度历史）：受 **本地元数据/数据源未齐** 影响，需补实体物理表 + 模板 `dataSourceId` 后再跑 Case 2.2、3.1、3.2、6.2。
- **建议复测**：配置 `equipment` 实体表与角色默认报表后，用 **5176** 端口重跑第三、四、六组。

---

## 关联自动测试

| 路径 | 说明 |
|------|------|
| `fe/src/pages/admin/reports/report-center.smoke.test.tsx` | Hub 渲染、筛选、预制深链 |
| `fe/src/pages/admin/reports/prefab-reports.smoke.test.tsx` | 预制列表、运行、深链 auto-run |
| `fe/src/pages/admin/reports/ReportViewPage.smoke.test.tsx` | 浏览页 auto-run、返回链 |
| `fe/src/pages/admin/reports/report-schedules.smoke.test.tsx` | 调度列表、历史重试 |
| `fe/src/pages/admin/reports/report-templates.smoke.test.tsx` | 模板树、扩展保存 |
| `tests/test_ff_rpt_companion_e95d.py` 等 | 后端 RPT 集成 |
