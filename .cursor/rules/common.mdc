---
description: VitalSpan 公共工程规范（分层、体量、公共代码防漂移）
alwaysApply: true
---

# 公共工程规范

> 栈专项：@.cursor/rules/backend-fastapi.mdc · @.cursor/rules/fe-ui.mdc  
> **路径与模块边界以 `docs/arch.md` 为准**；本文定义角色、依赖方向与体量，不重复栈细节。

## 体量（软约束）

| 范围 | 上限 | 说明 |
|------|------|------|
| 单函数 | ≤ **60** 行 | 超出拆私有函数或子模块 |
| 单文件（业务） | fe ≤ **300** 行；py ≤ **200** 行 | fe 细则见 `fe-ui.mdc` |
| 同目录手写源文件 | ≤ **12** 个 | 超出建子包/子目录 |
| **豁免** | — | Alembic 迁移、生成物、脚手架模板不计入；禁止为凑数搬文件 |

## 分层角色（先于路径）

按**职责**分层；目录名以 `docs/arch.md` §4 为准：

| 角色 | 职责 | VitalSpan 后端 | VitalSpan 前端 `fe/` |
|------|------|----------------|----------------------|
| **entry** | 路由/协议绑定，薄 | `app/api/v1/`、`main.py` | `routes.tsx`、`pages/**` 路由入口 |
| **use-case** | 编排、事务边界、调用域服务 | 域模块内 `service.py` 或薄 orchestrator | `hooks/`、页面内编排（M1 仅壳层可省略） |
| **domain** | 可复用业务规则、无 HTTP | `datasources/`、`query/`、`ingestion/`、`auth/`… | `components/`（业务块）、`lib/` 业务工具 |
| **shared** | 无状态横切能力 | `core/`（config、logging、middleware）、`schemas/` | `components/ui/`、`lib/utils.ts` |
| **outbound** | 第三方/出站代理 | `datasources/dialects/*`、外部 HTTP 客户端 | `@/lib/api.ts`（仅 HTTP 出站） |

**依赖速记**：

```
entry → use-case → domain
outbound → 外部系统（DB、总线、第三方 API）
```

- **禁止** outbound 直接触达 ORM 会话管理（连接池归 `datasources/`）
- **禁止** shared（`core/`）依赖上层 domain 业务逻辑
- **禁止** entry 写 SQL/方言/连接器细节

## 公共代码（防漂移）

| 层 | 清单路径 | 写 entry / 页面前 |
|----|----------|-------------------|
| 后端域 | `docs/services/README.md` + `docs/services/<域>.md` | 必读对应域附录 |
| 前端组件 | `fe/src/components/README.md` | 必读 |

| 规则 | 要求 |
|------|------|
| 复用 | 清单已有能力能覆盖 → **必须引用**；不够则扩 API，禁止 use-case/页面内复制 |
| 上浮 domain | 同领域逻辑 ≥2 处 → 域模块 `backend/app/<domain>/` 或 `fe/src/components/` |
| 上浮 helper | 仅同 entry 组内共用 → `api/v1/<域>/` 下私有模块或 `fe/pages/<page>/components/` |
| 上浮 shared | 跨模块、无业务状态 → `core/` / `schemas/` / `fe/src/lib/` |
| 维护 | 公共 API 变更：**同 PR** 更新 README / `docs/services/` + 测试 |
| 顺手 | 改 use-case 时发现重复 → 同 PR 抽取上浮 |

**禁止**：use-case 组互引私有 helper；shared import entry/domain 业务；entry 写业务 SQL；复制横切能力（响应包装、脱敏、traceId — 用 `core/` 已有模块）。

## 安全与文档

- 凭证：`CREDENTIAL_FERNET_KEY`（ADR-06）；日志/API 响应脱敏见 `backend-fastapi.mdc`
- 代码变更后文档同步：@.cursor/rules/prd-sync.mdc
- 修 bug 前检索：`.agents/skills/bug-case-library/cases/`

## 项目铁律（摘要）

| 项 | 约定 |
|----|------|
| 前端根目录 | **`fe/`**（禁止 `frontend/`） |
| API 前缀 | `/api/v1/`；健康检查 `/health` |
| 鉴权模块 | `backend/app/auth/`；**`main.py` 注册** `AuthMiddleware` |
| 公开路径 | `/health`、`/docs`、`/redoc`、`/openapi.json` |
| BI 运行时 | **零** Superset/DataEase 依赖（NFR-08） |

详述：@.cursor/rules/vitalspan-project.mdc
